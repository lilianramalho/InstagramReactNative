import {StyleSheet} from 'react-native';

const estilo = StyleSheet.create({
  fotoUsuario:{
    width: 45,
    height: 45,
    margin: 10,
    borderRadius: 50,
  },

  cabecalho:{
      flexDirection: "row",
      alignItems: "center",
  }
})

export default estilo;